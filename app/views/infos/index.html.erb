<h1>Listing infos</h1>

<table>
  <tr>
    <th>Product article</th>
    <th>Content</th>
    <th>Скидка</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @infos.each do |info| %>
  <tr>
    <td><%= info.product_article %></td>
    <td><%= info.content %></td>
    <td><%= info.discount %></td>
    <td><%= link_to 'Show', info %></td>
    <td><%= link_to 'Edit', edit_info_path(info) %></td>
    <td><%= link_to 'Destroy', info, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Info', new_info_path %>
